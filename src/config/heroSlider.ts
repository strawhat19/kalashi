// Set autoplay to false to stop automatic rotation for every HeroSlider.
// Individual instances can override this with <HeroSlider autoplay={false} />.
export const heroSliderConfig = {
  autoplay: true,
  intervalMs: 6500,
};
